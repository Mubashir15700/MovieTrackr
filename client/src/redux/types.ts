// Define your initial state interface (if needed)
export interface InitialState {
    // Define initial state properties here
}

// Define root state interface for Redux store
export interface RootState {
    // Define properties for each slice/reducer state here
}
